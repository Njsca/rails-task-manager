<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>
<h3><%= @task.completed %></h3>

<%= link_to "Edit this task", edit_task_path(@task.id) %>
<%= link_to "Delete this task", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are your sure?"} %>
<a href="/tasks">Back to tasks</a>
